spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://account-mysql:3306/users
    username: user
    password: ${MYSQL_PASSWORD}

server:
  context-path: /uaa
  port: 5000
